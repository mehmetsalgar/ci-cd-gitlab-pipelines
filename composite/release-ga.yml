release-ga:
  stage: deploy
  trigger:
    include:
      - project: 'org.salgar.fsm.akka/ci-cd-gitlab-pipelines'
        ref: 'development'
        file: '/composite/build.yml'
    strategy: depend
  rules:
    - if: $USE_CASE_8 == "true"
      when: manual
    - when: never