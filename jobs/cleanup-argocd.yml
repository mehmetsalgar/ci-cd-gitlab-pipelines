helm-delete:
  stage: deploy
  variables:
    BRANCH_NAME: $TARGET_BRANCH_NAME
    USE_CASE_3: "true"
  trigger:
    project: 'org.salgar.fsm.akka/fsm-akka-4eyes-argocd'
    branch: 'master'
    strategy: depend