helm-push:
  stage: deploy
  image: dtzar/helm-kubectl:3.11.2
  script:
    - |
      "helm registry login $HELM_URL --username $HELM_USER --password $HELM_PASSWORD"
      "helm push /builds/helm/charts/$CHART_NAME-$CHART_VERSION.tgz oci://$HELM_URL/helm"