helm-push:
  stage: deploy
  image: dtzar/helm-kubectl:3.11.2
  script:
    - |
      echo $HELM_PASSWORD | helm registry login $HELM_URL -u $HELM_USER --password-stdin
      helm push $CHART_NAME-$CHART_VERSION.tgz oci://$HELM_URL$HELM_PATH/helm
